import { ReaderShell } from "../components/reader/ReaderShell";

/**
 * PDF Reader page — Phase 2 shell only.
 * PDF.js integration arrives in Phase 6.
 */
export default function Reader() {
  return <ReaderShell />;
}
